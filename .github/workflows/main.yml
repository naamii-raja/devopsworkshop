name: Barani app on AWS
on:
  push:
    branches:
    - main
  
jobs:
  Build-App:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4  
      - name: Barani CI/CD
        run:
            echo "We are Learning About Devop's Today"
